<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de pedidos</title>
    <link rel="stylesheet" href="/css/orders.css">
</head>
<body>
    <header>
        <h1>Lista de pedidos</h1>
    </header>
    <main>
        <table border="1">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Dirección</th>
                    <th>Teléfono</th>
                    <th>Carrito</th>
                    <th>Estado</th>
                    <th>Fecha</th>
                </tr>
            </thead>
            <tbody>
                <% orders.forEach(order => { %>
                    <tr>
                        <td><%= order.id %></td>
                        <td><%= order.firstName %> <%= order.lastName %></td>
                        <td><%= order.address %></td>
                        <td><%= order.phone %></td>
                        <td>
                            <% 
                                const cart = JSON.parse(order.cart);
                                for (const [key, item] of Object.entries(cart)) {
                            %>
                                <p><%= item.name %> (x<%= item.quantity %>)</p>
                            <% } %>
                        </td>
                        <td><%= order.status %></td>
                        <td><%= new Date(order.createdAt).toLocaleString() %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
        <div class="actions">
            <button onclick="window.location.href='/products/list'"> << Volver</button>
        </div>
    </main>
    <footer>
        &copy; 2024 HydroMotion Store
    </footer>
</body>
</html>
